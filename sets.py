# With two sets of data, check for the similarities and differences between the two data sets
# Create another set with all the elements without duplicates
a = set([1, 2, 3, 4])
b = set([2, 10, 4, 9])
c = set()

